/**
 * FUNCTIONS
 * 
 * 0: 
 * 
 * 1: Two phases to using functions: "First we must ___. NExt we can execute (or two other words for executing a function) a function by ____.""
 * 
 * 2: Difference between a function's parameters and arguments passed into a function.
 * 
 * 3: Syntax of a named functions
 * 
 * 4: Assigning a function to a variable
 * 
 * 5: How to specify inputs and outputs. Functions can optionally take inputs and optionally return a single value. 
 * 
 * 6: Scope: "Functions can see and modify variables in parent or global scopes. The inverse is not true ... ___"
 * 
 * 7: Closures: "Functions form closures around the data they house. If an object returned from the Functions is held in memory somewhere (referenced),
 * that closure stays ALIVE and data can continue to exist in these closures "" 
 * 
 */